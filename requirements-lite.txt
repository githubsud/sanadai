# "Lite" deployment (e.g. Render free tier, 512 MB RAM): lexical search (SQLite FTS5) + Gemini/Claude extraction
# + Dorar. No neural retrieval models (torch / sentence-transformers / chromadb / onnxruntime).
# Run with USE_DENSE=false USE_RERANKER=false.
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30
pydantic>=2.7,<3
pydantic-settings>=2.3
httpx>=0.27
beautifulsoup4>=4.12
python-dotenv>=1.0
huggingface_hub>=0.24
anthropic>=0.40
