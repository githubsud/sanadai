# --- web / core ---
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30
pydantic>=2.7,<3
pydantic-settings>=2.3
httpx>=0.27
python-dotenv>=1.0

# --- LLM ---
anthropic>=0.40

# --- retrieval (heavy; CPU wheels are fine) ---
sentence-transformers>=3.0
torch>=2.3
chromadb>=0.5

# --- dev ---
pytest>=8.0
pytest-asyncio>=0.23
ruff>=0.6
