# --- web / core ---
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30
pydantic>=2.7,<3
pydantic-settings>=2.3
httpx>=0.27
beautifulsoup4>=4.12
python-dotenv>=1.0

# --- LLM ---
anthropic>=0.40

# --- retrieval (heavy; CPU wheels are fine) ---

torch>=2.3
chromadb>=0.5
# int8 ONNX inference on CPU (scripts/fetch_models.py); pins a transformers 4.x compatible sentence-transformers
sentence-transformers[onnx]>=5.0,<6
optimum[onnxruntime]>=1.23

# --- dev ---
pytest>=8.0
pytest-asyncio>=0.23
ruff>=0.6
